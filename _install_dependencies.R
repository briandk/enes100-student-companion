# Necessary for `bookdown::bs4_book()` output format
install.packages(c("bookdown",
                   "bslib",
                   "devtools",
                   "downlit",
                   "xml2"))

tinytex::install_tinytex()
